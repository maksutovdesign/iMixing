Drop your own demo `.wav` stems into this folder if you want to try the audio CLI locally.

This directory is intentionally empty in git, so `imixing-agent examples/stems` will fail until you add real files such as `kick.wav`, `bass.wav`, or `lead_vocal.wav`.
